on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, edited, synchronize, reopened]
jobs:
  Test_Result:
    runs-on: ubuntu-latest
    steps:
      - name: Print branch name
        if: github.event_name == 'pull_request'
        run: echo "${GITHUB_HEAD_REF}"
      
  

      - name: Remove Older Build pack
        env:
         HEROKU_TOKEN: ${{ secrets.HEROKU_API_KEY }}
        run: |
          echo $HEROKU_TOKEN
          curl -v -n -X PUT https://api.heroku.com/apps/650b637e-ffd2-45c5-bd24-11e7e49487d2/buildpack-installations -H "Content-Type: application/json" -H "Accept: application/vnd.heroku+json; version=3" -H  "Authorization: "Bearer $HEROKU_TOKEN"" -d '{"updates":[]}'
          

      - name: Set newer Heroku Buildpack 
        run: echo 'The branch name is' 

      - name: Trigger a herokubuild on sfdocs-help
        run: echo 'The branch name is' $BRANCH_NAME